# --- SERVER CONFIGURATION ---
server.port=8080 
server.servlet.context-path=/api 
server.address=0.0.0.0

# --- DATABASE CONFIGURATION (AIVEN PRODUCTION) ---
spring.datasource.url=jdbc:mysql://mysql-13161f4b-sreemani0323-3de4.d.aivencloud.com:10573/defaultdb?requireSSL=true&serverTimezone=UTC
spring.datasource.username=avnadmin
spring.datasource.password=${DB_PASSWORD}
spring.datasource.hikari.connection-timeout=60000

# --- JPA/HIBERNATE CONFIGURATION ---
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.open-in-view=false


logging.level.root=DEBUG
logging.level.org.hibernate.SQL=DEBUG
spring.jpa.properties.hibernate.globally_quoted_identifiers=true
# --- CORS POLICY (Final Frontend URL) ---
spring.web.cors.allowed-origins=https://sreemani0323.github.io
spring.web.cors.allowed-methods=POST,GET
spring.web.cors.allowed-headers=*
# Add these lines to application.properties
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always

# Optional: tune connection pool
spring.datasource.hikari.maximum-pool-size=10
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl