# --- SERVER CONFIGURATION ---
server.port=8080 
server.servlet.context-path=/api 
server.address=0.0.0.0

# --- DATABASE CONFIGURATION (AIVEN PRODUCTION - FINAL) ---
# NOTE: The socketTimeout is essential for overcoming cloud network latency issues.
spring.datasource.url=jdbc:mysql://mysql-13161f4b-sreemani0323-3de4.d.aivencloud.com:10573/defaultdb?requireSSL=true&serverTimezone=UTC&socketTimeout=60000
spring.datasource.username=avnadmin
spring.datasource.password=${DB_PASSWORD}

# --- JPA/HIBERNATE CONFIGURATION ---
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
# NOTE: We rely on Hibernate to auto-detect the dialect and naming strategy.

# --- CORS POLICY (Final Frontend URL) ---
spring.web.cors.allowed-origins=https://sreemani0323.github.io
spring.web.cors.allowed-methods=POST,GET
spring.web.cors.allowed-headers=*

# Optional: tune connection pool (CLEANED)
spring.datasource.hikari.maximum-pool-size=10